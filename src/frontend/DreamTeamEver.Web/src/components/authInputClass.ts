/** Shared classes for auth form controls (login, register, forgot password). */
export const authInputClassName = 'auth-input'

export const authPrimaryButtonClassName = 'auth-btn-primary'

export const authLinkClassName = 'auth-link'
